# 開發計畫

進行中的功能計畫放這裡，完成後移到 `archive/`。

## 流程

1. 計畫檔名：`YYYY-MM-DD-<feature-name>.md`
2. 計畫結構：**User Story → Spec → Tasks**
3. 功能完成後：移至 `docs/plans/archive/`，並更新 `docs/FEATURES.md` 與 `docs/CHANGELOG.md`
